---
description: Get status/details of a specific job
argument-hint: <job-id>
tools: ["Bash"]
---

Use the Bash tool to run:

```bash
converge get "$JOB_ID"
```

Display the full job configuration and metadata in JSON format.
