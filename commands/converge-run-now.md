---
description: Trigger an immediate task run
argument-hint: <job-id>
tools: ["Bash"]
---

Use the Bash tool to run:

```bash
converge run-now "$JOB_ID"
```

Notify when the job is scheduled and show its status.
