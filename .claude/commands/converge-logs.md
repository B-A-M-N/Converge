---
description: View logs for a task
argument-hint: <job-id> [--json]
allowed-tools: ["Bash"]
---

Use the Bash tool to run:

```bash
converge logs "$JOB_ID" $OPTIONS
```

Format the output clearly. If `--json` is provided, show raw JSON.
