---
description: Cancel a recurring task
argument-hint: <job-id>
allowed-tools: ["Bash"]
---

Use the Bash tool to run:

```bash
converge cancel "$JOB_ID"
```

Confirm the cancellation and show the result.
