---
description: Create a new recurring task
argument-hint: --every <interval> --task "<task>" [--cli <cli>] [--stop <json>]
allowed-tools: ["Bash"]
---

Use the Bash tool to run:

```bash
converge add "$ARGUMENTS"
```

Wait for the command to complete and show its output.
